import { Modal } from "react-bootstrap";

const CreateProduct = ({ show }) => {
  return (
    <Modal show={show}>
      <Modal.Body>Create product</Modal.Body>
    </Modal>
  );
};

export default CreateProduct;
