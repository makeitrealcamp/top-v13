import { StyleSheet } from "react-native-web";

export const styles = StyleSheet.create({
  profileImage: {
    backgroundColor: "blue",
    width: 200,
    height: 200,
  },

  profileForm: {},

  input: {
    borderWidth: 1,
    height: 30,
  },
});
